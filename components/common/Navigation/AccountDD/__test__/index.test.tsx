import { render, screen } from '@testing-library/react'
import { AccountDD } from '@/components/common'

describe('AccountDD', () => {
    test('> renders \'My Account\' button', () => {
        screen.debug()
    })
})