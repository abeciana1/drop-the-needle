import React from 'react'
import { AccountI } from '@/interfaces'

const AccountDD = ({
    name,
    image,
    email
}: AccountI) => {

    return (
        <React.Fragment>

        </React.Fragment>
    )
}

export default AccountDD