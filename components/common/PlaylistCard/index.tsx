import { DashPowerHourType } from "@/types" 
import Link from 'next/link'

const PlaylistCard = ({
    id,
    title,
    cover_image
}: DashPowerHourType) => {

    return (
        <></>
    )
}

export default PlaylistCard