import { TrackI } from '@/interfaces'

const Track = ({ song, user }: TrackI) => {
    console.log(song)
    return(
        <li>
            <details>

            </details>
        </li>
    )
}

export default Track